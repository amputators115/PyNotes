# 6. dir() получает список вех атрибутов и методов объекта.

print(dir('Msc'))

# 13. input()  удобный способ получить данные от пользователя

my_name = input('Введите ваше имя: ')

# 31. type() Функция type применяется в двух сценариях. Если передать один параметр, то она вернет тип этого объекта. Если же передать три параметра, то можно создать объект type.

print(type(2))

# 32. id() - индификатор объекта в памяти

print(id(2))
